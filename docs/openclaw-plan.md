# OpenClaw Plan

The v0.6.1 OpenClaw catalog entry is template-only.

Windows support is marked WSL2-recommended unless native Windows support is explicitly verified. The linked plan `examples/install-plans/windows-openclaw-wsl-plan.json` only previews WSL status and source-review steps.

No OpenClaw installer, bootstrap script, clone command, or package manager is run by v0.6.1.
