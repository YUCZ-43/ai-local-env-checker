# Hermes Agent Plan

The v0.6.1 Hermes Agent catalog entry is documentation/template-only.

The project does not assume a repository URL, install command, local checkout path, or token. Users must configure and review those details before any future setup flow.

The linked plan `examples/install-plans/windows-hermes-agent-plan.json` is dry-run-only and prints review guidance. It does not clone, install, delete, repair, or modify system settings.
