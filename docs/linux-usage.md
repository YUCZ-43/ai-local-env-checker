# Linux Usage

Run the Linux checker from the repository root:

```bash
bash scripts/linux/check-linux.sh --check-only --language en-US --timeout 10
```

The script is detection-only. It checks distro, package managers, shells, base tools, AI development tools, Docker, permissions, PATH, proxy settings, and TCP 443 network reachability.
