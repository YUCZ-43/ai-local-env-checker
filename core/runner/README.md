# Runner Core

Owns command execution for future repair or install workflows.

The runner must enforce install-plan approval, command logging, timeouts, and policy checks before executing anything that can modify the system.
