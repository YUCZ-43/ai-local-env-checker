{
  "id": "windows-safe-demo-plan",
  "toolId": "safe-demo",
  "platform": "windows",
  "action": "safe-demo",
  "description": "Safe v0.8.0 demo plan for exercising controlled LOW-risk allowlisted execution without installing, repairing, deleting, or changing system settings.",
  "commands": [
    {
      "id": "demo-write-output",
      "description": "Print a harmless demo message.",
      "shell": "powershell",
      "command": "Write-Output",
      "args": ["AI Local Environment Checker safe demo"],
      "workingDirectory": ".",
      "requiresAdmin": false,
      "riskLevel": "LOW",
      "timeoutSec": 5,
      "dryRunOnly": false,
      "confirmationRequired": true,
      "expectedResult": "Prints a harmless demo message.",
      "rollbackNote": "No rollback is required because the command does not modify system state.",
      "verificationCommands": ["Write-Output verification-ok"]
    },
    {
      "id": "demo-whoami",
      "description": "Print the current user name.",
      "shell": "powershell",
      "command": "whoami",
      "args": [],
      "workingDirectory": ".",
      "requiresAdmin": false,
      "riskLevel": "LOW",
      "timeoutSec": 5,
      "dryRunOnly": false,
      "confirmationRequired": true,
      "expectedResult": "Prints the current user name for local validation.",
      "rollbackNote": "No rollback is required because the command is read-only.",
      "verificationCommands": ["whoami"]
    },
    {
      "id": "demo-powershell-version",
      "description": "Print the current PowerShell version.",
      "shell": "powershell",
      "command": "$PSVersionTable.PSVersion",
      "args": [],
      "workingDirectory": ".",
      "requiresAdmin": false,
      "riskLevel": "LOW",
      "timeoutSec": 5,
      "dryRunOnly": false,
      "confirmationRequired": true,
      "expectedResult": "Prints the PowerShell version for local validation.",
      "rollbackNote": "No rollback is required because the command is read-only.",
      "verificationCommands": ["$PSVersionTable.PSVersion"]
    },
    {
      "id": "demo-git-version",
      "description": "Print Git version if Git is available.",
      "shell": "native",
      "command": "git",
      "args": ["--version"],
      "workingDirectory": ".",
      "requiresAdmin": false,
      "riskLevel": "LOW",
      "timeoutSec": 5,
      "dryRunOnly": false,
      "confirmationRequired": true,
      "expectedResult": "Prints the Git version if Git is already available.",
      "rollbackNote": "No rollback is required because the command is read-only.",
      "verificationCommands": ["git --version"]
    }
  ],
  "requiresAdmin": false,
  "riskLevel": "LOW",
  "confirmationRequired": true,
  "rollbackAvailable": false,
  "verificationCommands": [
    "Write-Output verification-ok",
    "whoami",
    "$PSVersionTable.PSVersion",
    "git --version"
  ],
  "autoExecute": false,
  "dryRunOnly": false,
  "expectedResult": "A local audit log and report are generated for harmless LOW-risk demo commands.",
  "rollbackNote": "No rollback is required because this plan performs no installation or system mutation.",
  "notes": [
    "This plan is intentionally harmless and exists only to validate the v0.8.0 controlled runner pipeline.",
    "Real execution still requires the explicit CLI --confirm flag."
  ]
}
