{
  "id": "windows-codex-cli-plan",
  "toolId": "codex-cli",
  "platform": "windows",
  "action": "install-codex-cli",
  "description": "Example future plan for installing Codex CLI on Windows. This file documents intended commands only and remains blocked for real execution in v0.8.0.",
  "commands": [
    {
      "id": "install-codex-cli",
      "description": "Future command for installing Codex CLI with npm.",
      "shell": "powershell",
      "command": "npm",
      "args": ["install", "-g", "@openai/codex"],
      "workingDirectory": ".",
      "requiresAdmin": false,
      "riskLevel": "MEDIUM",
      "timeoutSec": 300,
      "dryRunOnly": true,
      "confirmationRequired": true,
      "expectedResult": "Future installer would install Codex CLI if this risk class is enabled in a later version.",
      "rollbackNote": "Future rollback design must remove only files created by that installer and must not delete user projects.",
      "verificationCommands": ["codex --version"]
    },
    {
      "id": "verify-codex-cli",
      "description": "Future verification command for Codex CLI.",
      "shell": "powershell",
      "command": "codex",
      "args": ["--version"],
      "workingDirectory": ".",
      "requiresAdmin": false,
      "riskLevel": "LOW",
      "timeoutSec": 30,
      "dryRunOnly": true,
      "confirmationRequired": true,
      "expectedResult": "Future verification would print Codex CLI version if already installed.",
      "rollbackNote": "No rollback is required for read-only version checks.",
      "verificationCommands": ["codex --version"]
    }
  ],
  "requiresAdmin": false,
  "riskLevel": "MEDIUM",
  "confirmationRequired": true,
  "rollbackAvailable": false,
  "verificationCommands": [
    "codex --version"
  ],
  "autoExecute": false,
  "dryRunOnly": true,
  "expectedResult": "v0.8.0 validates, simulates, and dry-runs this plan but blocks confirmed execution.",
  "rollbackNote": "Rollback for real Codex CLI installation is not implemented in v0.8.0.",
  "notes": [
    "Example only. v0.8.0 must validate, simulate, and dry-run this plan, not execute it.",
    "npm install -g remains blocked by the safe demo allowlist."
  ]
}
