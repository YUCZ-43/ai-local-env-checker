{
  "id": "windows-node-git-vscode-plan",
  "toolId": "node-git-vscode",
  "platform": "windows",
  "action": "install-node-git-vscode",
  "description": "Example future plan for installing Node.js, Git, and VS Code on Windows. This file documents intended commands only and remains blocked for real execution in v0.8.0.",
  "commands": [
    {
      "id": "install-nodejs-lts",
      "description": "Future command for installing Node.js LTS with winget.",
      "shell": "powershell",
      "command": "winget",
      "args": ["install", "OpenJS.NodeJS.LTS"],
      "workingDirectory": ".",
      "requiresAdmin": true,
      "riskLevel": "HIGH",
      "timeoutSec": 600,
      "dryRunOnly": true,
      "confirmationRequired": true,
      "expectedResult": "Future installer would install Node.js LTS if admin and installer execution are enabled in a later version.",
      "rollbackNote": "Future rollback design must use the package manager uninstall path and must not delete user projects.",
      "verificationCommands": ["node --version", "npm --version"]
    },
    {
      "id": "install-git",
      "description": "Future command for installing Git with winget.",
      "shell": "powershell",
      "command": "winget",
      "args": ["install", "Git.Git"],
      "workingDirectory": ".",
      "requiresAdmin": true,
      "riskLevel": "HIGH",
      "timeoutSec": 600,
      "dryRunOnly": true,
      "confirmationRequired": true,
      "expectedResult": "Future installer would install Git if admin and installer execution are enabled in a later version.",
      "rollbackNote": "Future rollback design must use the package manager uninstall path and must not delete repositories.",
      "verificationCommands": ["git --version"]
    },
    {
      "id": "install-vscode",
      "description": "Future command for installing VS Code with winget.",
      "shell": "powershell",
      "command": "winget",
      "args": ["install", "Microsoft.VisualStudioCode"],
      "workingDirectory": ".",
      "requiresAdmin": true,
      "riskLevel": "HIGH",
      "timeoutSec": 600,
      "dryRunOnly": true,
      "confirmationRequired": true,
      "expectedResult": "Future installer would install VS Code if admin and installer execution are enabled in a later version.",
      "rollbackNote": "Future rollback design must use the package manager uninstall path and must not delete user settings.",
      "verificationCommands": ["code --version"]
    }
  ],
  "requiresAdmin": true,
  "riskLevel": "HIGH",
  "confirmationRequired": true,
  "rollbackAvailable": false,
  "verificationCommands": [
    "node --version",
    "npm --version",
    "git --version",
    "code --version"
  ],
  "autoExecute": false,
  "dryRunOnly": true,
  "expectedResult": "v0.8.0 validates, simulates, and dry-runs this plan but blocks confirmed execution.",
  "rollbackNote": "Rollback for real Node.js, Git, and VS Code installation is not implemented in v0.8.0.",
  "notes": [
    "Example only. v0.8.0 must validate, simulate, and dry-run this plan, not execute it.",
    "Administrator elevation is not auto-launched in v0.8.0.",
    "winget install remains blocked by the safe demo allowlist."
  ]
}
