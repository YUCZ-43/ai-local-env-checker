{
  "id": "windows-claude-code-plan",
  "platform": "windows",
  "action": "install-claude-code",
  "description": "Example future plan for installing Claude Code on Windows. This file documents intended commands only and must not be executed by the v0.5.0 runner.",
  "commands": [
    {
      "id": "install-claude-code",
      "description": "Future command for installing Claude Code with npm.",
      "shell": "powershell",
      "command": "npm",
      "args": ["install", "-g", "@anthropic-ai/claude-code"],
      "workingDirectory": ".",
      "requiresAdmin": false,
      "riskLevel": "MEDIUM",
      "timeoutSec": 300,
      "dryRunOnly": true,
      "confirmationRequired": true,
      "verificationCommands": ["claude --version"]
    },
    {
      "id": "verify-claude-code",
      "description": "Future verification command for Claude Code.",
      "shell": "powershell",
      "command": "claude",
      "args": ["--version"],
      "workingDirectory": ".",
      "requiresAdmin": false,
      "riskLevel": "LOW",
      "timeoutSec": 30,
      "dryRunOnly": true,
      "confirmationRequired": true,
      "verificationCommands": ["claude --version"]
    }
  ],
  "requiresAdmin": false,
  "riskLevel": "MEDIUM",
  "confirmationRequired": true,
  "rollbackAvailable": false,
  "verificationCommands": [
    "claude --version"
  ],
  "autoExecute": false,
  "dryRunOnly": true,
  "notes": [
    "Example only. v0.5.0 must validate and dry-run this plan, not execute it."
  ]
}
