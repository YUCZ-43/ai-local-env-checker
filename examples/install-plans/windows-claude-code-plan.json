{
  "id": "windows-claude-code-plan",
  "toolId": "claude-code",
  "platform": "windows",
  "action": "install-claude-code",
  "description": "Example future plan for installing Claude Code on Windows. This file documents intended commands only and remains blocked for real execution in v0.8.0.",
  "commands": [
    {
      "id": "install-claude-code",
      "description": "Future command for installing Claude Code with npm.",
      "shell": "powershell",
      "command": "npm",
      "args": ["install", "-g", "@anthropic-ai/claude-code"],
      "workingDirectory": ".",
      "requiresAdmin": false,
      "riskLevel": "MEDIUM",
      "timeoutSec": 300,
      "dryRunOnly": true,
      "confirmationRequired": true,
      "expectedResult": "Future installer would install Claude Code if this risk class is enabled in a later version.",
      "rollbackNote": "Future rollback design must remove only files created by that installer and must not delete user projects.",
      "verificationCommands": ["claude --version"]
    },
    {
      "id": "verify-claude-code",
      "description": "Future verification command for Claude Code.",
      "shell": "powershell",
      "command": "claude",
      "args": ["--version"],
      "workingDirectory": ".",
      "requiresAdmin": false,
      "riskLevel": "LOW",
      "timeoutSec": 30,
      "dryRunOnly": true,
      "confirmationRequired": true,
      "expectedResult": "Future verification would print Claude Code version if already installed.",
      "rollbackNote": "No rollback is required for read-only version checks.",
      "verificationCommands": ["claude --version"]
    }
  ],
  "requiresAdmin": false,
  "riskLevel": "MEDIUM",
  "confirmationRequired": true,
  "rollbackAvailable": false,
  "verificationCommands": [
    "claude --version"
  ],
  "autoExecute": false,
  "dryRunOnly": true,
  "expectedResult": "v0.8.0 validates, simulates, and dry-runs this plan but blocks confirmed execution.",
  "rollbackNote": "Rollback for real Claude Code installation is not implemented in v0.8.0.",
  "notes": [
    "Example only. v0.8.0 must validate, simulate, and dry-run this plan, not execute it.",
    "npm install -g remains blocked by the safe demo allowlist."
  ]
}
