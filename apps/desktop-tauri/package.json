{
  "name": "ai-local-env-checker-desktop",
  "version": "0.9.0",
  "description": "Admin permission approval UX local self-test for AI Local Environment Checker",
  "author": "AI Local Environment Checker maintainers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:frontend",
    "build:frontend": "tsc && vite build",
    "build:cli-bundle": "powershell -ExecutionPolicy Bypass -File ../../scripts/packaging/build-bundled-cli.ps1",
    "preview": "vite preview",
    "test": "vitest run",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.4"
  }
}
